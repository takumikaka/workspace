1、最左边的1是一个符号位，此处表明这个一个负数，负数在计算机中已补码的形式存在的。因此还需要换算成原码。
   将补码减1得到反码形式，再将反码取反就可以得到原码。
2、求余的运算时使用第一个操作数除以第二个操作数，得到一个整除的结果后剩下的值就是余数。
3、按位异或。当两位相同时返回0，不同时返回1。
4、private(当前类访问权限)，这个成员只能在当前类的内部被访问。
5、public(公共访问权限)，这个成员或外部类就可以被所有类访问。
